to download the tool
"git clone https://github.com/OBG001/-.git"

To send a message to all members of the private server, follow the instructions. 


The code works primarily by defining the basic variables, which is `msg` by specifying the message, such as: `msg = "Hello World"`.


The `guild` is defined by the type of server to which all members are to be sent the message.